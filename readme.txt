todo list:

create a system in objectInterface to read from a list of objects and produce them all (house-in-a-can)
create an entity class to handle stuff like players and other controllable things
make a small house with a pointlight inside
figure out how to local-server on a chromebook so you can add textures